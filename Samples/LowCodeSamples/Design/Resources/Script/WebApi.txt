<style>
  .code-view pre {
    font-family: Consolas, "Courier New", monospace;
    font-size: 16px;
    line-height: 1.3;
    margin: 20px;
    background: #ffffff;
  }
  .kw-blue  { color: #0000ff; }
  .kw-purple{ color: #800080; }
  .type-blue{ color: #0080c0; }
  .str      { color: #a31515; }
</style>

<div class="code-view">
<pre>
<span class="kw-blue">void</span> GetWeatherForecast_OnClick()
{
    <span class="kw-blue">var</span> data = <span class="type-blue">WebApiService</span>.Get(<span class="str">"/testapi"</span>).JsonObject;
    WeatherForecastList.DeleteAllRows();
    <span class="kw-purple">foreach</span>(<span class="kw-blue">var</span> e in data)
    {
        <span class="kw-blue">var</span> row = <span class="kw-blue">new</span> <span class="type-blue">WeatherForecast</span>();
        row.Date.Value = e.Date;
        row.TemperatureC.Value = e.TemperatureC;
        row.TemperatureF.Value = e.TemperatureF;
        row.Summary.Value = e.Summary;
        WeatherForecastList.AddRow(row);
    }    
}
</pre>
</div>
