-- 旧ワークフローサンプルは Codeer.LowCode.Blazor.Extras の承認フロー (ddl_approval.txt) に置き換えた。
-- user_master はビデオ会議サンプル (VideoConferencingAccount) が参照するため残す。

CREATE TABLE user_master (
    id SERIAL PRIMARY KEY,
    user_id TEXT,
    user_name TEXT
);
